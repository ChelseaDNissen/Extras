class Solution {
public:
    int maxProfit(vector<int>& prices) {
      
        int profit = 0;
        int price = prices[0];
        for (int& currentPrice : prices) {
            profit = max(profit, currentPrice - price);
          
            price = min(price, currentPrice);
        }
      
       
        return profit;
    }
};
